import { ParqueoFilterPipe } from './parqueo-filter.pipe';

describe('ParqueoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ParqueoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
