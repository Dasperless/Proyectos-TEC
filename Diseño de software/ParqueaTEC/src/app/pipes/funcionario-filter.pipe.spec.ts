import { FuncionarioFilterPipe } from './funcionario-filter.pipe';

describe('FuncionarioFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FuncionarioFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
