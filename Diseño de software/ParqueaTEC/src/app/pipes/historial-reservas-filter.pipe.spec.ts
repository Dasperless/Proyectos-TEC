import { HistorialReservasFilterPipe } from './historial-reservas-filter.pipe';

describe('HistorialReservasFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HistorialReservasFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
