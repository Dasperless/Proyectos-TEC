import { InformeFuncionariosPipe } from './informe-funcionarios.pipe';

describe('InformeFuncionariosPipe', () => {
  it('create an instance', () => {
    const pipe = new InformeFuncionariosPipe();
    expect(pipe).toBeTruthy();
  });
});
